# Dashboard-only (deployed to Vercel). Playwright and its transitive deps
# (greenlet, pyee) intentionally live in requirements-worker.txt instead -
# app.api never imports Playwright, and Vercel's docs are explicit that
# it doesn't tree-shake, so listing it here would just bloat every
# dashboard build/deploy for no reason.
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.2
click==8.4.2
et_xmlfile==2.0.0
fastapi==0.139.2
h11==0.16.0
idna==3.18
openpyxl==3.1.5
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.51.0

# Multi-user dashboard + Supabase backend
supabase==2.31.0
cryptography==49.0.0
Jinja2==3.1.6
python-multipart==0.0.32
itsdangerous==2.2.0
email-validator==2.3.0
