# Adds Playwright (+ its transitive deps) on top of the dashboard
# requirements, for anything that needs to actually drive a browser:
# worker/run_worker.py, app/scanner.py, app/whatsapp_session.py.
# Install this instead of requirements.txt when running the worker,
# locally or in the Docker image (see Dockerfile).
-r requirements.txt
playwright==1.61.0
greenlet==3.5.3
pyee==13.0.1
